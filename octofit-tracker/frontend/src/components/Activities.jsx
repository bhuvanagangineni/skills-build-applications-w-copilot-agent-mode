import { useEffect, useState } from 'react';
import { apiUrl } from '../api.js';
const API_URL = apiUrl('/api/activities/');
export default function Activities() { const [items, setItems] = useState([]); const [message, setMessage] = useState(''); const [form, setForm] = useState({ type: 'Run', duration: 20, points: 40, note: '' }); const load = () => fetch(API_URL).then((r) => r.json()).then((data) => setItems(Array.isArray(data) ? data : data.results || [])).catch(() => setMessage('Connect the API to load activities.')); useEffect(load, []); const submit = (event) => { event.preventDefault(); setMessage(''); fetch(API_URL, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(form) }).then((r) => { if (!r.ok) throw new Error(); return r.json(); }).then(() => { setMessage('Activity logged. Nice work.'); load(); }).catch(() => setMessage('Choose a seeded user before logging an activity.')); }; return <div className="page-wrap"><div className="page-intro"><div><span className="section-kicker">THE RECEIPTS</span><h2>Activity log</h2><p>Every session is a vote for the person you are becoming.</p></div><form className="quick-log" onSubmit={submit}><select value={form.type} onChange={(e) => setForm({ ...form, type: e.target.value })}><option>Run</option><option>Walk</option><option>Strength</option><option>Cycle</option><option>Swim</option></select><input type="number" min="1" value={form.duration} onChange={(e) => setForm({ ...form, duration: Number(e.target.value) })} aria-label="Minutes" /><button className="button button-coral">+ Log session</button></form></div>{message && <div className="notice">{message}</div>}<section className="panel table-panel"><div className="panel-heading"><h3>Recent sessions</h3><span className="count-badge">{items.length} sessions</span></div>{items.map((item) => <div className="table-row" key={item._id}><div className="activity-symbol">{item.type === 'Run' ? '↗' : '◒'}</div><strong>{item.type}</strong><span>{item.note || 'Movement session'}</span><span>{item.duration} min</span><b>{item.points} pts</b></div>)}{!items.length && <Empty text="No activity yet. Your next session can change that." />}</section></div>; }
function Empty({ text }) { return <p className="empty-state">{text}</p>; }